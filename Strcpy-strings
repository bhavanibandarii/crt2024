#include <stdio.h>
#include<string.h>
int main() {
    int i;
    // Write C code here
    int str1[50],str2[50];
    scanf("%s",str1);
    for(i=0;str1[i]!='\0';i++)
    {
        str2[i]=str1[i];
    }
    str2[i]='\0';
    printf("%s\n%s",str1,str2);
    return 0;
}
